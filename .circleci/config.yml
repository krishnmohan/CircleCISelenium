version: 2
jobs:
  build:
    docker:
      - image: circleci/<language>:<version TAG>
    steps:
    
    ## Set the artifacts
general:
  artifacts:
    - "./tests/screenshots" #Save the screenshots for the tests
 
## Customize dependencies
dependencies:
  pre:
    - sudo pip install -U selenium	
 
## Running Tests
test:
   override:
    - java  CircleCISelenium/CoreTest/src/FunctionalTest/CoreTestScript.java
